# Tree-Builder-Frontend

A React + TypeScript + Vite app for creating and managing tree nodes.

## Local setup

1. npm install
2. npm run dev

## Project structure

- src/features/tree: tree components, hooks, services, types
- src/services: api client, endpoints

## Notes

Resolved the merge conflict markers and kept the final project title.
